import { appsDepsGraphInterfaces } from './apps-deps-graph-interfaces';

describe('appsDepsGraphInterfaces', () => {
  it('should work', () => {
    expect(appsDepsGraphInterfaces()).toEqual('apps-deps-graph-interfaces');
  });
});
