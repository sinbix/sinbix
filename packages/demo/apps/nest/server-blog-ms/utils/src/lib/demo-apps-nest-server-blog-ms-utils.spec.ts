import { demoAppsNestServerBlogMsUtils } from './demo-apps-nest-server-blog-ms-utils';

describe('demoAppsNestServerBlogMsUtils', () => {
  it('should work', () => {
    expect(demoAppsNestServerBlogMsUtils()).toEqual(
      'demo-apps-nest-server-blog-ms-utils'
    );
  });
});
