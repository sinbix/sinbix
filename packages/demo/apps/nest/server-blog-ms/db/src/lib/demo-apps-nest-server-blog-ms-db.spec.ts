import { demoAppsNestServerBlogMsDb } from './demo-apps-nest-server-blog-ms-db';

describe('demoAppsNestServerBlogMsDb', () => {
  it('should work', () => {
    expect(demoAppsNestServerBlogMsDb()).toEqual(
      'demo-apps-nest-server-blog-ms-db'
    );
  });
});
