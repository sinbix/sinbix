import { demoAppsNestServerBlogMsServices } from './demo-apps-nest-server-blog-ms-services';

describe('demoAppsNestServerBlogMsServices', () => {
  it('should work', () => {
    expect(demoAppsNestServerBlogMsServices()).toEqual(
      'demo-apps-nest-server-blog-ms-services'
    );
  });
});
