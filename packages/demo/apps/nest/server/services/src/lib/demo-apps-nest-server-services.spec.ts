import { demoAppsNestServerServices } from './demo-apps-nest-server-services';

describe('demoAppsNestServerServices', () => {
  it('should work', () => {
    expect(demoAppsNestServerServices()).toEqual(
      'demo-apps-nest-server-services'
    );
  });
});
