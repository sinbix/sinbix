import { demoAppsNestServerAuthMsDb } from './demo-apps-nest-server-auth-ms-db';

describe('demoAppsNestServerAuthMsDb', () => {
  it('should work', () => {
    expect(demoAppsNestServerAuthMsDb()).toEqual(
      'demo-apps-nest-server-auth-ms-db'
    );
  });
});
