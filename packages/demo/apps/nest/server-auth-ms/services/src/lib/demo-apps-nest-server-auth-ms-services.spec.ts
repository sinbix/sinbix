import { demoAppsNestServerAuthMsServices } from './demo-apps-nest-server-auth-ms-services';

describe('demoAppsNestServerAuthMsServices', () => {
  it('should work', () => {
    expect(demoAppsNestServerAuthMsServices()).toEqual(
      'demo-apps-nest-server-auth-ms-services'
    );
  });
});
