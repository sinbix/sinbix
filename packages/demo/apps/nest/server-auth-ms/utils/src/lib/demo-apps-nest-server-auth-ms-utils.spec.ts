import { demoAppsNestServerAuthMsUtils } from './demo-apps-nest-server-auth-ms-utils';

describe('demoAppsNestServerAuthMsUtils', () => {
  it('should work', () => {
    expect(demoAppsNestServerAuthMsUtils()).toEqual(
      'demo-apps-nest-server-auth-ms-utils'
    );
  });
});
