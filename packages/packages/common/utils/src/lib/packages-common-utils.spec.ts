import { packagesCommonUtils } from './packages-common-utils';

describe('packagesCommonUtils', () => {
  it('should work', () => {
    expect(packagesCommonUtils()).toEqual('packages-common-utils');
  });
});
