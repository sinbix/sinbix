import { packagesSinbixUtilsPlugin } from './packages-sinbix-utils-plugin';

describe('packagesSinbixUtilsPlugin', () => {
  it('should work', () => {
    expect(packagesSinbixUtilsPlugin()).toEqual('packages-sinbix-utils-plugin');
  });
});
