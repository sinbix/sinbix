# <%= name %>

This library was generated with [Sinbix](https://sinbix.dev).
<% if (unitTestRunner === 'jest') { %>

## Running unit tests

Run `sinbix test <%= name %>` to execute the unit tests via [Jest](https://jestjs.io).

<% } %>
